// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shuTwT/hoshikuzu/ent/flinkgroup"
)

// FLinkGroup is the model entity for the FLinkGroup schema.
type FLinkGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 名称
	Name string `json:"name,omitempty"`
	// 描述
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FLinkGroupQuery when eager-loading is set.
	Edges        FLinkGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FLinkGroupEdges holds the relations/edges for other nodes in the graph.
type FLinkGroupEdges struct {
	// Links holds the value of the links edge.
	Links []*FLink `json:"links,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// LinksOrErr returns the Links value or an error if the edge
// was not loaded in eager-loading.
func (e FLinkGroupEdges) LinksOrErr() ([]*FLink, error) {
	if e.loadedTypes[0] {
		return e.Links, nil
	}
	return nil, &NotLoadedError{edge: "links"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FLinkGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case flinkgroup.FieldID:
			values[i] = new(sql.NullInt64)
		case flinkgroup.FieldName, flinkgroup.FieldDescription:
			values[i] = new(sql.NullString)
		case flinkgroup.FieldCreatedAt, flinkgroup.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FLinkGroup fields.
func (_m *FLinkGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case flinkgroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case flinkgroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case flinkgroup.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case flinkgroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case flinkgroup.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FLinkGroup.
// This includes values selected through modifiers, order, etc.
func (_m *FLinkGroup) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLinks queries the "links" edge of the FLinkGroup entity.
func (_m *FLinkGroup) QueryLinks() *FLinkQuery {
	return NewFLinkGroupClient(_m.config).QueryLinks(_m)
}

// Update returns a builder for updating this FLinkGroup.
// Note that you need to call FLinkGroup.Unwrap() before calling this method if this FLinkGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *FLinkGroup) Update() *FLinkGroupUpdateOne {
	return NewFLinkGroupClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the FLinkGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *FLinkGroup) Unwrap() *FLinkGroup {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: FLinkGroup is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *FLinkGroup) String() string {
	var builder strings.Builder
	builder.WriteString("FLinkGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteByte(')')
	return builder.String()
}

// FLinkGroups is a parsable slice of FLinkGroup.
type FLinkGroups []*FLinkGroup
